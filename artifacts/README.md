# Model Artifacts

Model artifacts are generated by the offline training pipeline.

Each immutable model version is stored under:

artifacts/versions/<artifact_version>/

A version contains:

- `model_bundle.joblib` — serialized inference models
- `metadata.json` — version metadata and artifact-bound inference parameters

The `artifacts/current` file points to the version currently used for inference.

Generated model artifacts are not committed to Git.